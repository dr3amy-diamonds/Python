# 🟩 ENUNCIADO — PRÁCTICA INTEGRADORA DE TKINTER (NIVEL INICIAL)

Desarrolla una aplicación gráfica de escritorio en Python usando **Tkinter** que cumpla con los siguientes requisitos:

---

## 📌 Requisitos generales

* La aplicación debe abrir una **ventana principal**.
* La ventana debe tener:

  * Un **título personalizado**.
  * Un **tamaño definido**.
* El programa debe ejecutarse desde un archivo **`.py`**.

---

## 📌 Elementos de la interfaz

La ventana debe contener:

* Un **texto inicial** (`Label`) que muestre un mensaje de bienvenida.
* Una **caja de texto** (`Entry`) donde el usuario pueda escribir su nombre.
* Un **botón** con un texto descriptivo (por ejemplo: *"Mostrar saludo"*).
* Un **segundo texto** (`Label`) que se usará para mostrar mensajes dinámicos.

---

## 📌 Funcionalidad

Al presionar el botón:

* El programa debe **leer el texto** ingresado por el usuario.
* Usando una **función**, debe **actualizar el segundo `Label`** mostrando un saludo personalizado con el nombre ingresado.
* Si el usuario **no escribe nada**, el programa debe mostrar un **mensaje alternativo** (por ejemplo: *"Por favor, ingrese un nombre"*).

---

## 📌 Restricciones

Debes usar únicamente:

* `Tk`
* `Label`
* `Entry`
* `Button`
* `pack()`

❌ No usar librerías externas.
❌ No usar código por terminal.
✅ Toda la interacción debe ser **gráfica**.

---

## 📌 Objetivo del ejercicio

Este ejercicio busca que practiques:

* Creación de ventanas con **Tkinter**
* Uso de **widgets básicos**
* Manejo de **eventos con botones**
* Uso de **funciones**
* Conexión entre **lógica y GUI**

---

## 📌 Nivel esperado

👉 **Principiante**, pero con buena base en Python.
👉 **No se evalúa diseño**, solo funcionalidad y comprensión.
