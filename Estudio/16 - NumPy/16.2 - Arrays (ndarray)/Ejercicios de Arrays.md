# 🟩 Ejercicios --- Arrays Básicos con NumPy

Este conjunto de ejercicios tiene como objetivo reforzar los conceptos
básicos sobre **arrays de NumPy**, su creación, propiedades y
comportamiento.

------------------------------------------------------------------------

## 🟢 Ejercicio 1 --- Crear un array 1D

Crea un array de NumPy que contenga los siguientes valores numéricos:

    [5, 10, 15, 20, 25]

Luego realiza las siguientes acciones:

-   Imprime el array.
-   Imprime el tipo del objeto usando `type`.
-   Imprime el tipo de dato interno del array usando `dtype`.

------------------------------------------------------------------------

## 🟢 Ejercicio 2 --- Dimensiones y forma del array

A partir del array creado en el ejercicio anterior:

-   Imprime el número de dimensiones del array.
-   Imprime su forma (`shape`).
-   Imprime el número total de elementos que contiene.

------------------------------------------------------------------------

## 🟢 Ejercicio 3 --- Crear un array 2D

Crea un array bidimensional que represente la siguiente matriz:

    1  2  3
    4  5  6

Luego:

-   Imprime el array completo.
-   Imprime el número de dimensiones (`ndim`).
-   Imprime la forma del array (`shape`).

------------------------------------------------------------------------

## 🟡 Ejercicio 4 --- Acceso a elementos de un array 2D

Utilizando el array 2D del ejercicio anterior:

-   Imprime el primer elemento del array.
-   Imprime el último elemento del array.
-   Imprime un elemento específico usando índices de fila y columna.

------------------------------------------------------------------------

## 🟡 Ejercicio 5 --- Homogeneidad de tipos en NumPy

Crea un array de NumPy con los siguientes valores:

    [1, 2, 3.7, 4]

Luego:

-   Imprime el array.
-   Imprime su `dtype`.
-   Observa y reflexiona qué tipo de dato eligió NumPy y por qué.

------------------------------------------------------------------------

## 🟡 Ejercicio 6 --- Modificación de valores

Crea un array con los valores:

    [100, 200, 300]

Luego:

-   Imprime el array original.
-   Cambia el valor del segundo elemento a `999`.
-   Imprime nuevamente el array después de la modificación.

------------------------------------------------------------------------

## 🟠 Ejercicio 7 --- Comparación entre lista y array

Crea:

-   Una **lista de Python** con valores numéricos.
-   Un **array de NumPy** con los mismos valores.

Luego:

-   Imprime ambos.
-   Observa y analiza las diferencias visuales y conceptuales entre una
    lista y un array.

------------------------------------------------------------------------

